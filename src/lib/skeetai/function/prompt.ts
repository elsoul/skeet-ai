import { Example } from '@/lib/types/skeetaiTypes'

export const functionPrompt = () => {
  const prompt: Example = {
    context: ``,
    examples: [
      {
        input: ``,
        output: ``,
      },
    ],
  }
  return prompt
}
